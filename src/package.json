{
  "name": "sls-config",
  "version": "0.0.1",
  "main": "./serverless.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --fix --cache"
  },
  "description": "serverless config component",
  "author": "wujohns",
  "license": "MIT",
  "dependencies": {}
}